#A script to update the server software and clean everything up
name: update 

run:
    - apt update
    - apt upgrade -y
    - apt autoremove -y
    - apt clean -y
    #- reboot

- test workflow
- python update workflow
- pip packages update workflow
- pip requirements install workflow

+ Added explantion for every worklow on the top